language: android

android:
  components:
    - tools
    - platform-tools
    - build-tools-28.0.3
    - extra-android-m2repository
    - extra-android-support
    - android-28

sudo: required

jdk:
  - oraclejdk8

jobs:
  include:
    - stage: build
      script: ./gradlew clean build -x test

    - stage: test
      script: ./gradlew test